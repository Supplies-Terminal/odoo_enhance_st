<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <template id="report_purchase_order_allocation">
            <t t-call="web.html_container">
                <style>
                    /* 设置第一列的固定宽度 */
                    .allocation-table th:first-child, .allocation-table td:first-child {
                        width: 100px; /* 根据需要调整宽度 */
                        text-align: right;
                    }
                    /* 可选：设置表格列内容对齐方式 */
                    .allocation-table th, .allocation-table td {
                        text-align: left;
                        padding: 5px; /* 可调整内边距增加间距 */
                    }
                </style>
                <t t-foreach="allocation_data" t-as="page">
                    <div class="o_company_#{company.id}_layout">
                        <t t-set="company" t-value="company"/>
                        <div t-attf-class="article o_report_layout_standard1 o_company_#{company.id}_layout {{  'o_layout_background' if company.layout_background in ['Geometric', 'Custom']  else  '' }}" t-attf-style="background-image: url({{ 'data:image/png;base64,%s' % company.layout_background_image.decode('utf-8') if company.layout_background_image and company.layout_background == 'Custom' else '/base/static/img/bg_background_template.jpg' if company.layout_background == 'Geometric' else ''}});" t-att-data-oe-model="o and o._name" t-att-data-oe-id="o and o.id" t-att-data-oe-lang="o and o.env.context.get('lang')">
                            <!-- 确保每个 pickup_job 开始新的一页 -->
                            <div class="page">
                                <h2>分流清单 - <t t-esc="page['pickup_job']"/></h2>
                                <t t-foreach="page['locations']" t-as="location_group">
                                    <strong>目标区域: <t t-esc="location_group['location']"/></strong>
                                    <table class="table table-condensed allocation-table">
                                        <tbody>
                                            <t t-foreach="location_group['items']" t-as="item">
                                                <tr>
                                                    <td width="50" style="text-align: right"><t t-esc="item['quantity']"/></td>
                                                    <td width="50"><t t-esc="item['unit']"/></td>
                                                    <td><t t-esc="item['product']"/></td>
                                                </tr>
                                            </t>
                                        </tbody>
                                    </table>
                                </t>
                            </div> <!-- 结束一个页面 -->
                        </div>
                    </div>
                </t>
            </t>
        </template>
        <record id="action_report_purchase_order_allocation" model="ir.actions.report">
            <field name="name">Purchase Order Allocation</field>
            <field name="model">purchase.order</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">odoo_enhance_st.report_purchase_order_allocation</field>
            <field name="print_report_name">'Purchase Order Allocation - %s' % (object.name)</field>
        </record>
    </data>
</odoo>
