<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="group_invoice_no_payment" model="res.groups">
            <field name="name">Billing without Payments</field>
            <field name="category_id" ref="base.module_category_accounting_accounting"/>
            <field name="comment">Can manage invoices, but cannot register, create, post, edit, cancel, delete, or reconcile payments.</field>
            <field name="implied_ids" eval="[(4, ref('account.group_account_invoice'))]"/>
        </record>
    </data>
</odoo>
